apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "float-manager-server.fullname" . }}-test-connection"
  labels:
    {{- include "float-manager-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "float-manager-server.fullname" . }}-api:{{ .Values.api.service.port }}']
  restartPolicy: Never
