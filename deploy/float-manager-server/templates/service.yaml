apiVersion: v1
kind: Service
metadata:
  name: {{ include "float-manager-server.fullname" . }}
  labels:
    {{- include "float-manager-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "float-manager-server.selectorLabels" . | nindent 4 }}
